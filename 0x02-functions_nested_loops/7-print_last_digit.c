#include <stdlib.h>
#include "main.h"
/**
 * print_last_digit - Print last digit of a number
 * @num: The number
 * Return: Always 0 (Success)
 */
int print_last_digit(int num)
{
int ld = num % 10;
return (ld);
}
