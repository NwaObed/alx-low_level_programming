#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * _islower - Checks lowercase characters
 * 
 * @c: The character to check
 * 
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
if islower(c)
return (1);
else
return (0);
}
