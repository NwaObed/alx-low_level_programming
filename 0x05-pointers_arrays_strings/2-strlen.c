#include "main.h"
#include <string.h>
/**
 * _strlen - To calculate the length of string pointed to by s
 * @s: pointer
 * Return - 1
 */
int _strlen(char *s)
{
	int str_len;

	str_len = strlen(s);
	return (str_len);
}

