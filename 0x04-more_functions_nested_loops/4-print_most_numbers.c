#include "main.h"
/**
 * print_most_numbers - Prints more numbers
 * * Return: Always 0 (Success)
 */
void print_most_numbers(void)
{
int i = 0;
while (i < 0)
{
if ((i == 2) || (i == 4))
continue;
else
_putchar(i);
}
_putchar('\n');
}
