#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * _isupper - checks the uppercase of character of c
 * @c: The character to check
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _isupper(int c)
{
if isupper(c)
{
_putchar('\n');
return (1);
}
else
_putchar('\n');
return (0);
}
